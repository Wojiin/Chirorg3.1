framework:
    rate_limiter:
        authentication:
            policy: sliding_window
            limit: 5
            interval: '15 minutes'
        refresh:
            policy: sliding_window
            limit: 20
            interval: '1 minute'
