framework:
    rate_limiter:
        authentication:
            policy: sliding_window
            limit: 5
            interval: '15 minutes'
        refresh:
            policy: sliding_window
            limit: '%env(int:JWT_REFRESH_RATE_LIMIT)%'
            interval: '1 minute'
